#pragma once

typedef enum location_Type {
    Frendly, 
    Ugly
} location_Type;

typedef enum game_State {
    fight, 
    wait
} game_State;

typedef enum fraction {
    people,
    goblin,
    dwarf
} fraction;